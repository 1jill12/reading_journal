<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <%= link_to "← Back to books", books_path, class: "text-gray-600 hover:text-gray-800" %>
  </div>

  <h1 class="text-4xl font-bold text-gray-800 mb-8">Add New Book</h1>

  <%= render "form", book: @book %>
</div>
